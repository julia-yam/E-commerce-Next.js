import {IconColor} from "@components/icons/Icon/configs";

export type ThemeToggleProps = {
  className?: string;
};

export const TOGGLE_CONFIG = {
  ICON_SIZE: 30,
  ICON_COLOR: "primary" as IconColor,
  ARIA_LABEL: "Toggle color theme",
};
